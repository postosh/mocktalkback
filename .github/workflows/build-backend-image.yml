name: Build Backend Image (GHCR)

on:
  push:
    branches:
      - main
    paths:
      - src/**
      - gradle/**
      - build.gradle
      - settings.gradle
      - Dockerfile
      - docker-compose.oci-deploy.yml
      - .github/workflows/build-backend-image.yml
      - .github/workflows/deploy-oci-backend.yml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: build-backend-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/mocktalkback
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: JDK 25 설정
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle

      - name: 백엔드 테스트 실행
        run: |
          chmod +x ./gradlew
          ./gradlew test

      - name: Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: GHCR 로그인
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 이미지 메타데이터 생성
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=prod-latest
            type=sha,format=short,prefix=sha-

      - name: 백엔드 이미지 빌드/푸시 (OCI용 amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: 배포 태그 요약
        run: |
          echo "배포 이미지: ${{ env.IMAGE_NAME }}:prod-latest" >> "$GITHUB_STEP_SUMMARY"
